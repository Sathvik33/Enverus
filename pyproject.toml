[project]
name = "multimodal-rag"
version = "1.0.0"
description = "Multimodal evidence-grounded RAG system for research papers"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
